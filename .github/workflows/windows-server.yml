name: Windows Server - RDP Only

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 0
    
    steps:
      - uses: actions/checkout@v4

      # ============================================
      # 1. INSTALL NETBIRD (LATEST VERSION)
      # ============================================
      - name: Install NetBird
        shell: pwsh
        run: |
          Write-Host "📥 Fetching latest NetBird release..."
          $release = Invoke-WebRequest -Uri "https://api.github.com/repos/netbirdio/netbird/releases/latest" -UseBasicParsing | ConvertFrom-Json
          $asset = $release.assets | Where-Object { $_.name -like "*windows_amd64.msi" } | Select-Object -First 1
          Invoke-WebRequest -Uri $asset.browser_download_url -OutFile "$env:TEMP\netbird.msi" -UseBasicParsing
          Start-Process msiexec -Wait -ArgumentList "/i `"$env:TEMP\netbird.msi`" /quiet /norestart"
          Write-Host "✅ NetBird installed"

      # ============================================
      # 2. CONNECT NETBIRD
      # ============================================
      - name: Connect NetBird
        env:
          NETBIRD_SETUP_KEY: ${{ secrets.NETBIRD_SETUP_KEY }}
        shell: pwsh
        run: |
          $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
          netbird up --setup-key $env:NETBIRD_SETUP_KEY
          Start-Sleep -Seconds 10
          netbird status

      # ============================================
      # 3. ENABLE RDP
      # ============================================
      - name: Enable RDP
        shell: pwsh
        run: |
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Write-Host "✅ RDP enabled"

      # ============================================
      # 4. FIND ADMIN ACCOUNT & SET PASSWORD
      # ============================================
      - name: Setup Admin Account
        shell: pwsh
        run: |
          $adminUser = Get-LocalUser | Where-Object { $_.SID -like "S-1-5-21-*-500" } | Select-Object -First 1
          if ($adminUser) { $adminName = $adminUser.Name } else { $adminName = "Administrator" }
          $Password = ConvertTo-SecureString "Root@2024!" -AsPlainText -Force
          Set-LocalUser -Name $adminName -Password $Password
          Enable-LocalUser -Name $adminName
          echo "ADMIN_USER=$adminName" >> $env:GITHUB_ENV

      # ============================================
      # 5. INSTALL 7-ZIP (OFFICIAL)
      # ============================================
      - name: Install 7-Zip
        shell: pwsh
        run: |
          $url = "https://www.7-zip.org/a/7z2409-x64.exe"
          Invoke-WebRequest -Uri $url -OutFile "$env:TEMP\7zip.exe" -UseBasicParsing
          Start-Process -FilePath "$env:TEMP\7zip.exe" -ArgumentList "/S" -Wait
          Write-Host "✅ 7-Zip installed"

      # ============================================
      # 6. GET NETBIRD IP
      # ============================================
      - name: Get NetBird IP
        shell: pwsh
        run: |
          $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
          $status = netbird status
          $ipLine = $status -split "`n" | Where-Object { $_ -match "NetBird IP:" -and $_ -notmatch "IPv6" } | Select-Object -First 1
          $NetBirdIP = ($ipLine -split "NetBird IP:")[1].Trim().Split("/")[0].Split(" ")[0]
          Write-Host "📡 NetBird IP: $NetBirdIP"
          echo "NETBIRD_IP=$NetBirdIP" >> $env:GITHUB_ENV

      # ============================================
      # 6.5 PROXY STEP (DELETE + CREATE, KEEP PORT)
      # ============================================
      - name: Configure NetBird Reverse Proxy (gitwin)
        env:
          NETBIRD_API_KEY: ${{ secrets.NETBIRD_API_KEY }}
        shell: pwsh
        run: |
          $token = $env:NETBIRD_API_KEY
          $base  = "https://api.netbird.io"
          $h     = @{ Authorization = "Bearer $token"; Origin = "https://app.netbird.io"; Referer = "https://app.netbird.io/" }

          $svcName  = "gitwin.rs4.indevs.in"
          $destIp   = $env:NETBIRD_IP
          $destPort = 3389
          $mode     = "tcp"

          Write-Host "Runner IP: $destIp"

          # ---- peer id from ip ----
          $tid = $null; $w = 0
          while ($w -lt 90) {
              try {
                  $peers = @(Invoke-RestMethod -Uri "$base/api/peers" -Headers $h)
                  $p = $peers | Where-Object { $_.ip -contains $destIp } | Select-Object -First 1
                  if ($p -and $p.connected -eq $true) { $tid = [string]$p.id; break }
              } catch {}
              Start-Sleep -Seconds 5; $w += 5
          }
          if (-not $tid) { throw "Peer $destIp not connected!" }
          Write-Host "Peer OK: $tid" -ForegroundColor Green

          # ---- find gitwin ----
          $proxyUrl = $null
          $list = @(Invoke-RestMethod -Uri "$base/api/reverse-proxies/services" -Headers $h)
          $hits = @($list | Where-Object { $_.name -eq $svcName -or $_.domain -eq $svcName })
          $routed = $hits | Where-Object { $t = @($_.targets); ($t.Count -gt 0) -and ([string]$t[0].host -eq $destIp) } | Select-Object -First 1

          if ($routed) {
              # already routed -> keep it, clean duplicates
              $rid = [string]$routed.id
              try {
                  $full = Invoke-RestMethod -Uri "$base/api/reverse-proxies/services/$rid" -Headers $h -Method GET -ErrorAction Stop
                  $proxyUrl = "$($full.domain):$($full.listen_port)"
              } catch {
                  $proxyUrl = [string]$routed.domain
              }
              Write-Host "✅ Already routed. Keeping $proxyUrl" -ForegroundColor Green
              foreach ($dup in $hits) {
                  $did = [string]$dup.id
                  if ($did -ne $rid) {
                      Invoke-RestMethod -Uri "$base/api/reverse-proxies/services/$did" -Headers $h -Method DELETE -ErrorAction SilentlyContinue | Out-Null
                  }
              }
          } else {
              # delete old (carry its port), create new for this IP
              $oldPort = $null
              if ($hits.Count -gt 0) {
                  $fid = [string]$hits[0].id
                  try {
                      $full = Invoke-RestMethod -Uri "$base/api/reverse-proxies/services/$fid" -Headers $h -Method GET -ErrorAction Stop
                      if ($full.PSObject.Properties['listen_port']) { $oldPort = $full.listen_port }
                  } catch {
                      Write-Host "Could not read old port -> auto port." -ForegroundColor Yellow
                  }
                  foreach ($m in $hits) {
                      $mid = [string]$m.id
                      Write-Host "Deleting old $mid" -ForegroundColor Yellow
                      Invoke-RestMethod -Uri "$base/api/reverse-proxies/services/$mid" -Headers $h -Method DELETE -ErrorAction SilentlyContinue | Out-Null
                  }
                  Start-Sleep -Seconds 3
              }

              $attempt = 1; $useOld = ($null -ne $oldPort)
              while ($true) {
                  $pl = if ($useOld) { [string]$oldPort } else { "auto" }
                  Write-Host "CREATE attempt $attempt (port $pl)..."
                  $body = [ordered]@{
                      name=$svcName; domain=$svcName; mode=$mode; enabled=$true; private=$false
                      pass_host_header=$false; rewrite_redirects=$false; terminated=$false
                      proxy_cluster="eu1.netbird.services"; auth=[pscustomobject]@{}
                      port_auto_assigned=(-not $useOld)
                      targets=@( [ordered]@{ enabled=$true; host=$destIp; port=[int]$destPort; protocol=$mode; target_type="peer"; target_id=$tid; options=[pscustomobject]@{} } )
                  }
                  if ($useOld) { $body.listen_port = [int]$oldPort }

                  $newId = $null
                  try {
                      $r = Invoke-RestMethod -Uri "$base/api/reverse-proxies/services" -Headers $h -Method POST -ContentType "application/json; charset=utf-8" -Body ($body | ConvertTo-Json -Depth 10) -ErrorAction Stop
                      $newId = [string]$r.id
                  } catch {
                      Write-Host "POST failed: $($_.ErrorDetails.Message)" -ForegroundColor Red
                      if ($useOld -and $attempt -ge 2) { $useOld = $false }
                      Start-Sleep -Seconds 5; $attempt++; continue
                  }

                  Start-Sleep -Seconds 5
                  $status = "error"
                  $check = $null
                  try {
                      $check = Invoke-RestMethod -Uri "$base/api/reverse-proxies/services/$newId" -Headers $h -Method GET -ErrorAction Stop
                      $status = [string]$check.meta.status
                  } catch {}

                  if ($status -eq "error") {
                      Write-Host "Status ERROR, retry..." -ForegroundColor Red
                      try { Invoke-RestMethod -Uri "$base/api/reverse-proxies/services/$newId" -Headers $h -Method DELETE -ErrorAction SilentlyContinue | Out-Null } catch {}
                      Start-Sleep -Seconds 5; $attempt++
                  } else {
                      $proxyUrl = "$($check.domain):$($check.listen_port)"
                      Write-Host "✅ SUCCESS -> $destIp`:$destPort | $proxyUrl" -ForegroundColor Green
                      break
                  }
              }
          }

          Write-Host "🌐 PROXY URL: $proxyUrl" -ForegroundColor Cyan
          echo "PROXY_URL=$proxyUrl" >> $env:GITHUB_ENV

      # ============================================
      # 7. CONNECTION INFO (shows gitwin URL + port)
      # ============================================
      - name: Connection Info
        run: |
          echo "=========================================="
          echo "✅ WINDOWS SERVER READY"
          echo "=========================================="
          echo ""
          echo "📡 NetBird IP: ${{ env.NETBIRD_IP }}"
          echo ""
          echo "🌐 PROXY URL: ${{ env.PROXY_URL }}"
          echo ""
          echo "🖥️ RDP (port 3389):"
          echo "  Address: ${{ env.NETBIRD_IP }}:3389"
          echo "  User: ${{ env.ADMIN_USER }}"
          echo "  Password: Root@2024!"
          echo ""
          echo "📦 Installed: 7-Zip"
          echo ""
          echo "👑 User: ${{ env.ADMIN_USER }} / Password: Root@2024!"
          echo "♾️ Server runs UNLIMITED TIME"
          echo "=========================================="

      # ============================================
      # 8. KEEP ALIVE (UNLIMITED)
      # ============================================
      - name: Keep Alive (Unlimited)
        run: |
          echo "=========================================="
          echo "🚀 WINDOWS SERVER RUNNING"
          echo "=========================================="
          echo ""
          echo "✅ NetBird: CONNECTED"
          echo "✅ RDP: ACTIVE on 3389"
          echo "✅ 7-Zip: INSTALLED"
          echo "🌐 Proxy: ${{ env.PROXY_URL }}"
          echo ""
          echo "♾️ Keeping alive forever..."
          while ($true) { Start-Sleep -Seconds 60 }